与数据库进行数据交互时，需要转换一下数据类型。比如字符串与数字之间转换、int与long之间转换等。

$count会抛出空指针异常，如果为null时，无法转换为Long。

Restriciton在page后，无法再次count，应该是内部结构发生了变化。

分页有问题，功能定义不清晰。

懒加载部分必须有缓存支持。

对外暴露的ResultSet应该改为Map，ResultSet只应该出现在底层JDBC处。

原生SQL语句：支持in数组，自动将数组展开，且可以不需要type参数。

发生SQL异常时，需要抛弃Connection并创建新的。

Slorm本身不需要缓存数据库连接，任何时候需要数据库连接，可直接从数据库连接池中获取。

与数据库进行数据交互时，需要转换一下数据类型。比如字符串与数字之间转换、int与long之间转换等。

$count会抛出空指针异常，如果为null时，无法转换为Long。

Restriciton在page后，无法再次count，应该是内部结构发生了变化。

首次并发操作时，容易出现javassist异常，这是因为生成代理类的工具类没有做并发处理，需要优化。

分页有问题，功能定义不清晰。